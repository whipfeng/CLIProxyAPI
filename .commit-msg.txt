fix: strip billing-header from client requests in Claude executor

Claude Code client sends x-anthropic-billing-header in system[] with
dynamic fields (cch=, cc_version) that change every request. Even after
removing our own injection, the client's own billing-header still breaks
KV cache for Kimi and other providers. Add stripBillingHeaderFromSystem()
to filter these blocks before sending upstream.
