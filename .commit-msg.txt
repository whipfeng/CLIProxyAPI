fix: prevent empty system[] after stripping billing-header (causes 400 error)

When all system blocks were billing headers, stripping them produced an empty
array which Kimi rejected with 400 Invalid request. Keep a minimal empty
text block when system would become empty.
